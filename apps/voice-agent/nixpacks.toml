# Nixpacks configuration for Railway deployment
# Force Python detection for voice-agent

[phases.setup]
nixPkgs = ["python312", "gcc", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python -m src.main"
